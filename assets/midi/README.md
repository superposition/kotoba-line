# MIDI Assets

This directory is reserved for optional local MIDI files.

Do not commit copyrighted station MIDI files. Station metadata may point at a
local placeholder path, and the audio planner must fall back to silent visual
pulses when the file or MIDI tooling is missing.
